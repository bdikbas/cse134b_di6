---
const links = [
  { href: "/", label: "Home" },
  { href: "/players", label: "Players" },
];
---

<nav>
  <ul>
    {links.map((link) => (
      <li><a href={link.href}>{link.label}</a></li>
    ))}
  </ul>
</nav>
