---
const { name, team, position, href, blurb, image } = Astro.props;
---

<article class="player-card">
{image && (
      <img
        src={image}
        alt={`Photo of ${name}`}
        class="player-card-image"
        loading="lazy"
      />
    )}
  <h2><a href={href}>{name}</a></h2>
  <p><strong>Team:</strong> {team}</p>
  <p><strong>Position:</strong> {position}</p>
  <p>{blurb}</p>
</article>
